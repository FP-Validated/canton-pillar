# Pillar

Stripe for Canton-backed assets.

See `docs/Architecture/` for design, `docs/Dev/` for the phased implementation plan.

## Quickstart

Requires Node 20.18, pnpm 9.12.3, JDK 21, Gradle 8.10.2, Daml SDK 3.4.11, Docker.

```
make bootstrap
make dev-up
```

See `CONTRIBUTING.md`.
