export async function startGoogleSignIn(returnTo='/dashboard'){const base=process.env.PILLAR_API_URL??'http://localhost:3001/v1';const r=await fetch(`${base}/auth/oauth/google/start`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({return_to:returnTo}),cache:'no-store'});if(!r.ok)throw new Error('oauth_start_failed');return r.json() as Promise<{authorization_url:string;state:string}>}
