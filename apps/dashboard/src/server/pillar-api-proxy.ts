export async function pillarApi(path: string, init: RequestInit = {}) { const base = process.env.PILLAR_API_URL ?? 'http://localhost:8080/v1'; const token = process.env.PILLAR_SERVER_TOKEN ?? 'dev-server-token'; const res = await fetch(`${base}${path}`, { ...init, headers: { 'authorization': `Bearer ${token}`, 'content-type':'application/json', ...(init.headers ?? {}) }, cache:'no-store' }); if (!res.ok) throw new Error(`Pillar API ${res.status}`); return res.json(); }
