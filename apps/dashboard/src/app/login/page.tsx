import { redirect } from 'next/navigation'; import { startGoogleSignIn } from '../../server/auth/google';
async function signIn(){'use server'; const { authorization_url } = await startGoogleSignIn('/dashboard'); redirect(authorization_url)}
export default function LoginPage(){return <main className='mx-auto flex min-h-screen max-w-md flex-col items-center justify-center gap-6 p-8 text-center'><h1 className='text-3xl font-semibold'>Sign in to Canton Pillar</h1><p className='text-slate-600'>Use Google OAuth to access your tenant dashboard.</p><form action={signIn}><button className='rounded-xl bg-slate-950 px-5 py-3 font-medium text-white'>Continue with Google</button></form></main>}
