import Link from "next/link";
import { getFeed } from "../lib/incidents";
export default function Page() { const feed = getFeed(); return <main className="mx-auto max-w-5xl p-8"><header className="rounded-2xl bg-white p-8 shadow"><p className="text-sm font-semibold uppercase tracking-wide text-slate-500">Canton Pillar</p><h1 className="mt-2 text-4xl font-bold">Status</h1><p className="mt-4 text-lg">Current status: <span className="font-semibold">{feed.current_status}</span></p></header><section className="mt-8 grid gap-4">{feed.incidents.map((incident) => <Link key={incident.id} href={`/incidents/${incident.id}`} className="rounded-xl bg-white p-6 shadow"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">{incident.title}</h2><span className="rounded-full bg-slate-100 px-3 py-1 text-sm">{incident.status}</span></div><p className="mt-3 text-slate-600">{incident.summary}</p></Link>)}</section></main>; }
