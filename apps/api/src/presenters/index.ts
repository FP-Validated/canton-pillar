import { PillarError } from '../errors/pillar-error.js';
import * as S from '@pillar/api-contracts/schemas';
export function present(schema: { safeParse(v:unknown): { success:boolean; data?:unknown; error?:unknown }}, value: unknown) { const r=schema.safeParse(value); if(!r.success) throw PillarError.api('Presenter validation failed.'); return r.data as any; }
export const presentAccount=(v:unknown)=>present(S.AccountSchema,v); export const presentAsset=(v:unknown)=>present(S.AssetSchema,v); export const presentBalance=(v:unknown)=>present(S.BalanceSchema,v); export const presentHolding=(v:unknown)=>present(S.HoldingSchema,v); export const presentIssueIntent=(v:unknown)=>present(S.IssueIntentSchema,v); export const presentRedeemIntent=(v:unknown)=>present(S.RedeemIntentSchema,v); export const presentTransferIntent=(v:unknown)=>present(S.TransferIntentSchema,v); export const presentHold=(v:unknown)=>present(S.HoldSchema,v); export const presentOperation=(v:unknown)=>present(S.OperationSchema,v); export const presentEvent=(v:unknown)=>present(S.EventSchema,v); export const presentWebhookEndpoint=(v:unknown)=>present(S.WebhookEndpointSchema,v); export const presentApiKey=(v:unknown)=>present(S.ApiKeySchema,v); export const presentApiKeyCreate=(v:unknown)=>present(S.ApiKeyCreateResponse,v); export const presentWebhookSecret=(v:unknown)=>present(S.RotateSecretResponse,v);
