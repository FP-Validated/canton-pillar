import { ResourceDetailShell } from '@/components/dashboard/ResourceDetailShell';
import { getOperationById, operations } from '@/lib/mockData';
export function generateStaticParams(){return operations.map((item)=>({id:item.id}));}
export default function Page({params}:{params:{id:string}}){const item=getOperationById(params.id); if(!item) return null; return <ResourceDetailShell section="Operations" id={item.id} status={item.status} record={item} attributes={[{label:'ID',value:item.id,copy:item.id},{label:'Resource',value:item.resource_id,copy:item.resource_id},{label:'Resource type',value:item.resource_type},{label:'Latency',value:`${item.latency_ms}ms`},{label:'Opaque update reference',value:item.ledger.update_reference},{label:'Updated',value:item.updated}]} related={[{label:'Resource detail',href:`/dashboard/${item.resource_type.includes('intent')?'intents':item.resource_type+'s'}/${item.resource_id}`,meta:item.resource_type}]} />;}
