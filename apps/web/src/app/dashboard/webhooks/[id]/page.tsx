import { ResourceDetailShell } from '@/components/dashboard/ResourceDetailShell';
import { getWebhookEndpointById, webhookEndpoints } from '@/lib/mockData';
export function generateStaticParams(){return webhookEndpoints.map((item)=>({id:item.id}));}
export default function Page({params}:{params:{id:string}}){const item=getWebhookEndpointById(params.id); if(!item) return null; return <ResourceDetailShell section="Webhooks" id={item.id} status={item.status} record={item} attributes={[{label:'ID',value:item.id,copy:item.id},{label:'URL',value:item.url},{label:'Description',value:item.description},{label:'Events',value:item.enabled_events.join(', ')},{label:'Secret suffix',value:item.secret_last4},{label:'Created',value:item.created}]} related={[{label:'Deliveries',href:'#',meta:'latest attempts'}]} />;}
