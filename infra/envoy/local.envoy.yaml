static_resources:
  listeners:
    - name: local_http
      address:
        socket_address: { address: 0.0.0.0, port_value: 80 }
      filter_chains:
        - filters:
            - name: envoy.filters.network.http_connection_manager
              typed_config:
                '@type': type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                stat_prefix: pillar_local
                server_header_transformation: PASS_THROUGH
                request_id_extension:
                  typed_config:
                    '@type': type.googleapis.com/envoy.extensions.request_id.uuid.v3.UuidRequestIdConfig
                    pack_trace_reason: false
                route_config:
                  name: pillar
                  virtual_hosts:
                    - name: api
                      domains: ['*']
                      routes:
                        - match: { path: /healthz }
                          route: { cluster: api, prefix_rewrite: /v1/health }
                        - match: { prefix: /v1/ }
                          route: { cluster: api, timeout: 30s, retry_policy: { retry_on: 5xx, num_retries: 3 } }
                http_filters:
                  - name: envoy.filters.http.local_ratelimit
                    typed_config:
                      '@type': type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit
                      stat_prefix: pillar_local_rate_limit
                      token_bucket: { max_tokens: 1000, tokens_per_fill: 1000, fill_interval: 60s }
                      filter_enabled: { default_value: { numerator: 100, denominator: HUNDRED } }
                      filter_enforced: { default_value: { numerator: 100, denominator: HUNDRED } }
                  - name: envoy.filters.http.router
                    typed_config:
                      '@type': type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
  clusters:
    - name: api
      connect_timeout: 2s
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      http2_protocol_options: {}
      circuit_breakers:
        thresholds:
          - max_connections: 1024
            max_pending_requests: 256
            max_retries: 3
      load_assignment:
        cluster_name: api
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address: { address: api, port_value: 4000 }
