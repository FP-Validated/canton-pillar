apiVersion: v1
kind: ConfigMap
metadata: { name: {{ include "pillar.fullname" . }}-billing-adapter }
data:
  DEPLOYMENT_MODE: {{ .Values.deploymentMode | quote }}
  STREAM_BACKEND: {{ .Values.billingAdapter.streamBackend | default "redis" | quote }}
