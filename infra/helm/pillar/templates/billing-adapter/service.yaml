apiVersion: v1
kind: Service
metadata: { name: {{ include "pillar.fullname" . }}-billing-adapter }
spec: { selector: { app.kubernetes.io/name: billing-adapter }, ports: [{ name: http, port: 8080, targetPort: http }] }
