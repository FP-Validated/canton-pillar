{{- $svc := index .Values "docs" -}}
{{- if $svc.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pillar-docs
spec:
  selector: { app: pillar-docs }
  ports: [{ name: http, port: {{ $svc.port }}, targetPort: http }]
{{- end }}
