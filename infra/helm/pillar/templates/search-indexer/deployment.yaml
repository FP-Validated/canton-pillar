apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pillar.fullname" . }}-search-indexer
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: search-indexer } }
  template:
    metadata: { labels: { app.kubernetes.io/name: search-indexer } }
    spec:
      containers:
        - name: search-indexer
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: {{ include "pillar.fullname" . }}-db, key: url } }
