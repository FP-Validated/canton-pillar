{{- $svc := index .Values "onboarding" -}}
{{- if $svc.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-pillar-onboarding
data:
  NODE_ENV: production
  PILLAR_API_URL: {{ $.Values.config.apiBaseUrl | quote }}
{{- end }}
