{{- $svc := index .Values "onboarding" -}}
{{- if $svc.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pillar-onboarding
spec:
  selector: { app: pillar-onboarding }
  ports: [{ name: http, port: {{ $svc.port }}, targetPort: http }]
{{- end }}
