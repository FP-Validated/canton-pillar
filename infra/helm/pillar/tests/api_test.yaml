suite: api deployment /v1 ingress
templates:
  - templates/api/deployment.yaml
tests:
  - it: renders Deployment
    asserts:
      - containsDocument:
          kind: Deployment
          apiVersion: apps/v1
          name: RELEASE-NAME-pillar-api
