suite: external secret runtime refs
templates:
  - templates/externalsecret.yaml
tests:
  - it: renders ExternalSecret
    asserts:
      - containsDocument:
          kind: ExternalSecret
          apiVersion: external-secrets.io/v1
          name: RELEASE-NAME-pillar-runtime
