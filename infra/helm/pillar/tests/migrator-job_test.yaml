suite: migrator blocks release
templates:
  - templates/migrator-job.yaml
tests:
  - it: renders Job
    asserts:
      - containsDocument:
          kind: Job
          apiVersion: batch/v1
          name: RELEASE-NAME-pillar-migrator
